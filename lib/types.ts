export interface registerType {
  full_name: string;
  phone_number: string;
  email: string;
  address: string;
  password: string;
}

export interface loginType{
    email:string;
    password:string;
}